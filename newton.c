#include <stdio.h> // header file for Standard Input Output
#include <stdlib.h> // header file for Standard Library

int main() {
    int N;
    if ( 0<=N>=1000)
    {
    printf("Sample Input 1\n ");
    scanf("%d", &N);
     if (N==1)
     { printf("Sample output 1\n AC",N);
     }
     else {printf("Sample output 2\n WC",N);}
    } 
	
	return 0;
}
